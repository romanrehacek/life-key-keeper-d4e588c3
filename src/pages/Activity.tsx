
import React from 'react';
import PageTemplate from '@/components/PageTemplate';
import ActivityCheck from '@/components/ActivityCheck';

const Activity = () => {
  return (
    <PageTemplate title="Kontrola aktivity">
      <ActivityCheck />
    </PageTemplate>
  );
};

export default Activity;
